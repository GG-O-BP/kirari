# Gleam build artifacts
*.beam
*.ez
/build
erl_crash.dump
mem

# kir artifacts
/.kir/store
kir.lock

# Claude Code local settings (shared settings are tracked)
.claude/settings.local.json

manifest.toml
gleam.toml
