name = "kirari"
version = "1.0.0"

description = "A unified package manager for Gleam — Hex + npm in one workflow"
licences = ["MPL-2.0"]
repository = { type = "github", user = "GG-O-BP", repo = "kirari" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"

[dev_dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
